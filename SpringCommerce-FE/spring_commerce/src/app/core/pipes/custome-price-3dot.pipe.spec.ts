import { CustomePrice3dotPipe } from './custome-price-3dot.pipe';

describe('CustomePrice3dotPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomePrice3dotPipe();
    expect(pipe).toBeTruthy();
  });
});
